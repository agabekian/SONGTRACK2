@model FormWrapper
<div class="form-box">
    <div class="row">
        <h3 class="text-warning">Edit Entry</h3>
        <div class=col-sm-12>
            <form 
                asp-action="UpdateRec" 
                asp-route-id="@Model.RecForm.RecordisId" 
                asp-controller="Home"
                asp-method="post"
            >
                    <partial name="_form1" model="@Model.RecForm" />
            </form>
            <partial name="_editEntry" model=@Model.ConnectForm />
        </div>
    </div>
</div>